# Keycloak authorization sample

#### Docker
Using the image from quay.io/keycloak/keycloak
```
docker-compose up
```

#### Standard

Install the dependencies and start NodeJS example by running:

```
npm install
npm start
```

Open the browser at http://localhost:3000/ and login with keycloak.
